enum ERoutes {
  dashboard = '/',
  pipelines = '/pipelines',
  rankers = '/rankers',
}

export enum ERouteNames {
  dashboard = 'Dashboard',
  pipelines = 'Pipelines',
  rankers = 'Rankers',
}

export default ERoutes;